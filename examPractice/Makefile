.PHONY: clean All

All:
	@echo "----------Building project:[ swapPtr - Debug ]----------"
	@cd "copyPtr" && "$(MAKE)" -f  "swapPtr.mk"
clean:
	@echo "----------Cleaning project:[ swapPtr - Debug ]----------"
	@cd "copyPtr" && "$(MAKE)" -f  "swapPtr.mk" clean
